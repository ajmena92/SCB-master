import { useState } from "react"; import { Button } from "@/components/ui/button"; import { api, errMsg } from "@/lib/api"; import type { LoteSalida, Previsualizacion } from "@/compartido/contratos/api";
export default function Importaciones() { const [archivo, setArchivo] = useState<File>(); const [vista, setVista] = useState<Previsualizacion>(); const [lote, setLote] = useState<LoteSalida>(); const [error, setError] = useState(""); const enviar = async (ruta: string) => { if (!archivo) return; try { setError(""); const f = new FormData(); f.append("archivo", archivo); const r = await api.post<Previsualizacion | LoteSalida>(ruta, f); if (ruta.includes("previsualizaciones")) setVista(r.data as Previsualizacion); else setLote(r.data as LoteSalida); } catch (e) { setError(errMsg(e)); } }; return <main className="space-y-6 p-6"><h1 className="text-2xl font-semibold">Importaciones</h1><input type="file" accept=".csv" onChange={e => setArchivo(e.target.files?.[0])} aria-label="Archivo CSV" />{error && <p role="alert">{error}</p>}<div className="flex gap-2"><Button onClick={() => void enviar("/v1/importaciones/previsualizaciones")} disabled={!archivo}>Previsualizar</Button><Button onClick={() => void enviar("/v1/importaciones/lotes")} disabled={!archivo}>Ejecutar importación</Button></div>{vista && <section aria-label="Resultado de previsualización"><p>{vista.totalFilas} filas; {vista.valida ? "válida" : "con errores"}.</p><ul>{vista.errores.map(e => <li key={`${e.fila}-${e.mensaje}`}>Fila {e.fila}: {e.mensaje}</li>)}</ul></section>}{lote && <p role="status">Lote #{lote.idLote}: {lote.estado} ({lote.totalFilas} filas)</p>}</main>; }

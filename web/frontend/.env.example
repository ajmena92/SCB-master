# Public configuration embedded at build time. Production should retain /api.
VITE_API_BASE_URL=/api

# Development server only. It preserves same-origin requests by proxying /api.
# API_PROXY_TARGET=http://127.0.0.1:8000

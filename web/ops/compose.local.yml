# Sobrescritura local. Debe combinarse después de compose.production.yml.
name: scb-web-local
services:
  postgres:
    restart: "no"
  migracion:
    command: ["upgrade", "head"]
    restart: "no"
  api:
    restart: "no"
  web:
    restart: "no"

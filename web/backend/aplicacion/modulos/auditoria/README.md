# Auditoría

Registra eventos de cambios de la plataforma web y permite consultarlos con el permiso `auditoria.leer`. El servicio es un puerto reutilizable por los módulos; no depende de WinForms ni de estructuras heredadas.

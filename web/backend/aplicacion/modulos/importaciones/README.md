# Importaciones

Valida archivos CSV UTF-8, ofrece una previsualización acotada y registra cada carga como lote web. La reversión cambia el estado del lote; no interpreta PIAD ni sincroniza dominios. Requiere `importaciones.leer` para consultar y `importaciones.ejecutar` para cargar o revertir.
